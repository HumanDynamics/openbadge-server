from .settings import * #@UnusedWildImport

DEBUG = True

SEND_POST_MEETING_SURVEY = False

PIPELINE_LESS_BINARY = "/Users/craig/.node/bin/lessc"
PIPELINE_YUI_BINARY = '/usr/local/bin/yuicompressor'

LOGGING['loggers'] = {
        '': {
            'handlers': ['logfile', 'console'],
            'propagate': True,
            'level': 'WARN',
        }
    }


# this might be necessary if you have different passwords, want to use a different database, or have less than mysql 5.7
# if you have mysql >= 5.7 you need 'SET default_storage_engine=INNODB'
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql', # Add 'postgresql_psycopg2', 'mysql', 'sqlite3' or 'oracle'.
        'NAME': passwords.MYSQL_DB,
        # The following settings are not used with sqlite3:
        'USER': passwords.MYSQL_USER,
        'PASSWORD': passwords.MYSQL_PASSWORD,
        'HOST': '', # Empty for localhost through domain sockets or '127.0.0.1' for localhost through TCP.
        'PORT': '', # Set to empty string for default.
        'OPTIONS': {
            'init_command': 'SET storage_engine=INNODB',
        }
    }
}
